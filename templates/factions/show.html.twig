{% extends 'base.html.twig' %}

{% block title %}Factions{% endblock %}

{% block body %}
    <h1>Factions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faction.id }}</td>
            </tr>
            <tr>
                <th>Faction</th>
                <td>{{ faction.faction }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ faction.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_factions_index') }}">back to list</a>

    <a href="{{ path('app_factions_edit', {'id': faction.id}) }}">edit</a>

    {{ include('factions/_delete_form.html.twig') }}
{% endblock %}
