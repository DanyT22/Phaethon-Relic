{% extends 'base.html.twig' %}

{% block title %}Raretes{% endblock %}

{% block body %}
    <h1>Raretes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rarete.id }}</td>
            </tr>
            <tr>
                <th>Rarete</th>
                <td>{{ rarete.rarete }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ rarete.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_raretes_index') }}">back to list</a>

    <a href="{{ path('app_raretes_edit', {'id': rarete.id}) }}">edit</a>

    {{ include('raretes/_delete_form.html.twig') }}
{% endblock %}
