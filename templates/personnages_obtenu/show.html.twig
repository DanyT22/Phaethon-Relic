{% extends 'base.html.twig' %}

{% block title %}PersonnagesObtenu{% endblock %}

{% block body %}
    <h1>PersonnagesObtenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnages_obtenu.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_personnages_obtenu_index') }}">back to list</a>

    <a href="{{ path('app_personnages_obtenu_edit', {'id': personnages_obtenu.id}) }}">edit</a>

    {{ include('personnages_obtenu/_delete_form.html.twig') }}
{% endblock %}
