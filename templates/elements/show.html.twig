{% extends 'base.html.twig' %}

{% block title %}Elements{% endblock %}

{% block body %}
    <h1>Elements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ element.id }}</td>
            </tr>
            <tr>
                <th>Element</th>
                <td>{{ element.element }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ element.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_elements_index') }}">back to list</a>

    <a href="{{ path('app_elements_edit', {'id': element.id}) }}">edit</a>

    {{ include('elements/_delete_form.html.twig') }}
{% endblock %}
