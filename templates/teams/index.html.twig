{% extends 'base.html.twig' %}

{% block title %}Teams index{% endblock %}

{% block body %}
    <div class="filtre"> </div>

    <a href="{{ path('app_teams_new') }}">
    <div class="bg-black flex items-center justify-center w-full p-8 m-8">
        <p class="text-center text-2xl text-white">Create new</p>
    </div>
</a>

    <div class="">
        {% for team in teams %}
            <a href="{{ path('app_team_show', {'id': team.id}) }}">
                <div>
                    <div>
                        <img href="{{ team.teamMember.build.personnage.icon }}">
                        <img href="{{ team.teamMember.build.moteur.icon }}">
                        <img>set1
                        <img>set2
                        <img>set3
                    </div>
                    <div>
                        <img href="{{ team.teamMember.build.personnage.icon }}">
                        <img href="{{ team.teamMember.build.moteur.icon }}">
                        <img>set1
                        <img>set2
                        <img>set3
                    </div>
                    <div>
                        <img href="{{ team.teamMember.build.personnage.icon }}">
                        <img href="{{ team.teamMember.build.moteur.icon }}">
                        <img>set1
                        <img>set2
                        <img>set3
                    </div>
                    <div>
                        <p> {{team.nomTeam}} </p>
                        <img href="{{ team.bangboos.icon }}">
                    </div>
                </div>
            </a>
        {% else %}
        <p>Aucune Team creer</p>
        {% endfor %}    
        </div>


{% endblock %}
