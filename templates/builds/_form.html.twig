{{ form_start(form, { 'attr': {'class': 'flex flex-col gap-8 w-full text-white'}}) }}

    <div class='flex flex-col md:flex-row items-center justify-center gap-4 w-full'>
        <div class='bg-black flex flex-col items-center justify-center gap-2 w-full md:w-1/2 p-2'>
            <p>Personnage</p>
            {{ form_row(form.personnage, {'label': false}) }}
            <img src="{{ asset('images/profils/Anby_Demara.jpg')}}" class="h-80">
        </div>

        <div class='bg-black flex flex-col items-center justify-center gap-2 w-full md:w-1/2 p-2'>
            <p>Moteur</p>
            {{ form_row(form.moteur, {'label': false})}}
            <img src="{{ asset('images/moteurs/[Lunaire]_Pleine_lune.jpg')}}" class="h-40">
        </div>
    </div>

    <div class="flex flex-col md:flex-row items-center justify-center gap-4 w-full">
        <div class="flex flex-col items-center justify-center gap-4 w-full md:1/2">
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 1</p>
                {{ form_row(form.disque1)}}
            </div>
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 2</p>
                {{ form_row(form.disque2)}}
            </div>
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 3</p>
                {{ form_row(form.disque3)}}
            </div>
        </div>
        <div class="flex flex-col items-center justify-center gap-4 w-full md:1/2">
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 4</p>
                {{ form_row(form.disque4)}}
            </div>
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 5</p>
                {{ form_row(form.disque5)}}
            </div>
            <div class='bg-black flex flex-col items-center justify-center gap-2 w-full p-2'>
                <p>Disque 6</p>
                {{ form_row(form.disque6)}}
            </div>
        </div>
    </div>

    <div class="bg-black flex items-center justify-center w-full p-8 my-4">
        <button class="btn">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
