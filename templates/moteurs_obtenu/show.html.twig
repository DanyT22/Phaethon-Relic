{% extends 'base.html.twig' %}

{% block title %}MoteursObtenu{% endblock %}

{% block body %}
    <h1>MoteursObtenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moteurs_obtenu.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moteurs_obtenu_index') }}">back to list</a>

    <a href="{{ path('app_moteurs_obtenu_edit', {'id': moteurs_obtenu.id}) }}">edit</a>

    {{ include('moteurs_obtenu/_delete_form.html.twig') }}
{% endblock %}
