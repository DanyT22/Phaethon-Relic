{% extends 'base.html.twig' %}

{% block title %}MoteursObtenu index{% endblock %}

{% block body %}
    <h1>MoteursObtenu index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for moteurs_obtenu in moteurs_obtenus %}
            <tr>
                <td>{{ moteurs_obtenu.id }}</td>
                <td>
                    <a href="{{ path('app_moteurs_obtenu_show', {'id': moteurs_obtenu.id}) }}">show</a>
                    <a href="{{ path('app_moteurs_obtenu_edit', {'id': moteurs_obtenu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_moteurs_obtenu_new') }}">Create new</a>
{% endblock %}
