{% extends 'base.html.twig' %}

{% block title %}Compte{% endblock %}

{% block body %}
    <div class="bg-black flex items-center justify-center w-full p-8 my-8">
        <h1 class="text-center text-2xl text-white">Compte utilisateur</h1>
    </div>

    {{ include('user/_form.html.twig', {'button_label': 'Mise a jour'}) }}

    <a href="{{ path("app_logout") }}"> Deco </a>

    <div class="bg-red-600 flex items-center justify-center w-full p-8 my-8">
        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
